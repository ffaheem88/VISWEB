Imports System
Imports System.Net.Mail
Imports System.Net
Imports System.IO
Imports System.Configuration


Partial Class contact
    Inherits System.Web.UI.Page

    'Protected Sub submit_Click(sender As Object, e As System.EventArgs) Handles submit.Click
    '    'SendHTMLMail()

    '    Const ToAddress As String = "syeda.muniza@jcrvis.com.pk"

    '    '(1) Create the MailMessage instance
    '    Dim mm As New MailMessage(email.Text, ToAddress)

    '    '(2) Assign the MailMessage's properties
    '    mm.Subject = name.Text
    '    mm.Body = msg.Text
    '    mm.IsBodyHtml = False

    '    '(3) Create the SmtpClient object
    '    Dim smtp As New SmtpClient

    '    '(4) Send the MailMessage (will use the Web.config settings)
    '    smtp.Send(mm)
    'End Sub


    
End Class
